// Este es el punto de entrada de tu aplicacion

/* import { register, logIn } from './app.js';

window.onload = initialHash;

function initialHash() {
  window.location.hash = '#login';
}

const root = document.getElementById('root');
const btnLogIn = document.getElementById('login');
//const viewToRegister = document.getElementById('register');

btnRegister.addEventListener('click'); {
  window.location.hash = '#register';
}

const viewToRegister = () => {
  window.location.hash = '/register';
  main.innerHTML = '';
  main.innerHTML = `<form class="registerUser">
  <input id="name" placeholder="ingrese su nombre">
  <input id="name" placeholder="ingrese su seudonimo o apodo"></input>
  <input id="emailRegister" type="email" placeholder="Ingrese su email">
  <input id="passwordRegister"   type="password" placeholder="Ingrese su contraseña">
  <button id="btnregister">Enviar</button>
  </form>`;
}; */
